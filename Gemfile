# Gemfile (minimal, compatible with github-pages)
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# For local `bundle exec jekyll serve` on Ruby ≥ 3:
gem "webrick", "~> 1.7"

gem 'connection_pool', '2.5.0'
